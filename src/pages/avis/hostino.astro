---
import Layout from "../../layouts/Layout.astro";
import AvisReview from "../../components/AvisReview.astro";
---

<Layout title="Avis Hostino - Hébergeur Marocain">
    <section class="hostino-hero-section">
        <div class="hostino-hero-bg-overlay" aria-hidden="true"></div>

        <div class="hostino-hero-container">
            <!-- Left Column: Text Content -->
            <div class="hostino-hero-content">
                <h1 class="hostino-hero-title-small">Hostino</h1>
                <h2 class="hostino-hero-title-main">Avis Hostino® 2025</h2>
            </div>

            <!-- Right Column: Logo/Image -->
            <div class="hostino-hero-image-col">
                <img
                    fetchpriority="high"
                    decoding="async"
                    width="1024"
                    height="236"
                    src="/images/hostino-wh-1024x236.png"
                    alt="Logo Hostino Maroc"
                    class="hostino-hero-img"
                />
            </div>
        </div>
    </section>
    <AvisReview />
</Layout>

<style>
    .hostino-hero-section {
        position: relative;
        background-color: #0d1b39;
        overflow: hidden;
        transition:
            background 0.3s,
            border 0.3s,
            border-radius 0.3s,
            box-shadow 0.3s;
        margin-top: 0;
        margin-bottom: 0;
        padding: 100px 0;
        z-index: 1;
    }

    .hostino-hero-bg-overlay {
        position: absolute;
        top: 0; 
        left: 0;
        width: 100%;
        height: 100%;
        background-image: url("/images/hero-bg.jpg");
        background-position: center center;
        background-repeat: no-repeat;
        background-size: cover;
        opacity: 0.14;
        mix-blend-mode: multiply;
        transition:
            background 0.3s,
            border-radius 0.3s,
            opacity 0.3s;
        z-index: 0;
        pointer-events: none;
    }

    @media (min-width: 1025px) {
        .hostino-hero-bg-overlay {
            background-attachment: fixed;
        }
    }

    .hostino-hero-container {
        position: relative;
        z-index: 2;
        display: flex;
        align-items: center;
        max-width: 1140px;
        margin: 0 auto;
        padding: 0 20px;
    }

    .hostino-hero-content {
        flex: 0 0 45%;
        max-width: 45%;
        padding-right: 60px;
    }

    .hostino-hero-title-small {
        color: #ffffff;
        font-family: "Poppins", sans-serif;
        font-size: 18px;
        font-weight: 500;
        line-height: 28px;
        margin: 0 0 6px 0;
    }

    .hostino-hero-title-main {
        color: #ffffff;
        font-family: "Poppins", sans-serif;
        font-size: 54px;
        font-weight: 700;
        line-height: 55px;
        margin: 0;
    }

    .hostino-hero-image-col {
        flex: 0 0 55%;
        max-width: 55%;
        display: flex;
        justify-content: flex-end;
        align-items: center;
    }

    .hostino-hero-img {
        width: 36%;
        height: auto;
        display: block;
    }

    /* Responsive overrides */
    @media (max-width: 1024px) {
        .hostino-hero-section {
            padding: 150px 15px;
        }

        .hostino-hero-content {
            flex: 0 0 50%;
            max-width: 50%;
            padding-right: 15px;
        }

        .hostino-hero-image-col {
            flex: 0 0 50%;
            max-width: 50%;
            padding-left: 15px;
        }

        .hostino-hero-title-small {
            font-size: 41px;
            line-height: 1.1em;
        }

        .hostino-hero-title-main {
            font-size: 41px;
            line-height: 1.1em;
        }
    }

    @media (max-width: 767px) {
        .hostino-hero-section {
            padding: 50px 20px;
        }

        .hostino-hero-bg-overlay {
            background-position: bottom right;
        }

        .hostino-hero-container {
            flex-direction: column;
            gap: 24px;
            text-align: center;
        }

        .hostino-hero-content,
        .hostino-hero-image-col {
            flex: 0 0 100%;
            max-width: 100%;
            padding: 0;
        }

        .hostino-hero-title-small {
            font-size: 14px;
            line-height: 1.4;
            margin: 0 0 8px;
        }

        .hostino-hero-title-main {
            font-size: 32px;
            line-height: 1.2;
            margin: 0;
        }

        .hostino-hero-image-col {
            justify-content: center;
        }

        .hostino-hero-img {
            width: 60%;
            max-width: 260px;
        }
    }
</style>